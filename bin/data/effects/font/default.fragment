#version 330 core

in vec2 UV;
in vec4 OColor;

uniform sampler2D Diffuse;



void main()
{

	vec4 blend = texture2D( Diffuse, vec2(UV.x,UV.y) );
	blend = blend*OColor;
	if(blend.a < 0.1)
		discard;

	gl_FragData[0] = blend;
	gl_FragData[1] = (vec4(-1,0,0,0)+1)/2;
}